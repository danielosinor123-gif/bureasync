// BureauSync frontend config for split deployment
// Set window.API_BASE to your API host when frontend (Vercel) and API (Render/Fly) are on different domains.
// Example: window.API_BASE = "https://bureasync-api.onrender.com";
// Leave empty for same-origin (local dev or Docker monolith).
window.API_BASE = window.API_BASE || "";
